var config = {
  db: {
    username: "root",
    password: "hussain",
    db_name: "emp_management",
    host: "localhost"
  },
  server: {
    host: "localhost",
    port: "3000"
  }
}

module.exports = config;
